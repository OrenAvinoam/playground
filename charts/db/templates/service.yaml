apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    {{- include "db.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "db.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5432
